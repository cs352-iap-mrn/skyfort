using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttackTower : Tower
{
    public AttackTower(int cost, string name, int range, int damage, double attackSpeed) : base(cost, name, range, damage, attackSpeed)
    {
    }
}
